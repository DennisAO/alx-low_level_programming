#include <stdio.h>

/**
 * main - the  prints all the numbers of base 16 in lowercase
 *
 * Return: Always zero
 */
int main(void)
{
	int a;

	for (a = 0 ; a <= 16 ; a++)
	{
		putchar(a);
	}
	putchar('\n');
		return (0);
}
