#include "main.h"

/**
 * _isdigit - is the name of the parameter
 * Return: if a digit is checked from zero to nine 1 is returned else 0
 * @c: this is the int number
 */
int _isdigit(int c)
{
	if (c >= 0 && c <= 9)
	{
	return (1);
	}
	else
	{
	return (0);
	}
}
