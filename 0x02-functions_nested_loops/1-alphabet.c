#include "main.h"

/**
 * print_alphabet - from a to z on lower case
 * Return: return void or zero on this
 */

void print_alphabet(void)
{
	char p;

	for (p = 'a' ; p <= 'z' ; p++)
		_putchar('p');
	_putchar('\n');
}
